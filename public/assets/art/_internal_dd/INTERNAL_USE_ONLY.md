# Private prototype assets

The images in this directory were derived from the user's locally installed
copy of *Darkest Dungeon*.

They are part of this private validation project and are synchronized with the
private repository for cross-device development. Rebuild locally with
`tools/build_demo_placeholders.py` when the source assets change.
